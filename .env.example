# Server
ECHO_HOST=0.0.0.0
ECHO_PORT=3888
# local: phone reaches this computer directly and insert happens inside the server process.
# relay: public server queues insert jobs; a desktop agent polls and pastes them.
ECHO_MODE=local
# Public HTTPS URL used in relay deployments, usually your domain.
# ECHO_PUBLIC_URL=https://voice.example.com
# Used by the desktop agent in internet relay mode.
# ECHO_RELAY_URL=https://voice.example.com
# Set this to a long random value if you want a stable pairing URL.
# ECHO_TOKEN=
# Android browsers require a secure context for microphone access.
# Option A: run `npm run android:usb` and open the localhost URL on the phone.
# Option B: provide a trusted HTTPS certificate and open the HTTPS LAN URL.
# HTTPS_CERT=/absolute/path/to/cert.pem
# HTTPS_KEY=/absolute/path/to/key.pem

# Speech-to-text provider: auto | openai | local | none
STT_PROVIDER=auto
STT_LANGUAGE=zh
STT_PROMPT=这是一段用于给 AI 助手、文档或聊天输入的中文或中英混合口述。请尽量保留专有名词、技术词和上下文，不要添加不存在的内容。

# OpenAI transcription. Current OpenAI speech-to-text models include
# gpt-4o-transcribe, gpt-4o-mini-transcribe, whisper-1, and diarization variants.
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_TRANSCRIBE_MODEL=gpt-4o-mini-transcribe

# Local/self-hosted STT. Examples:
#   Whisper ASR Webservice: http://192.168.1.10:9000/asr
#   OpenAI-compatible local endpoint: http://192.168.1.10:9000/v1/audio/transcriptions
LOCAL_STT_URL=
LOCAL_STT_FILE_FIELD=audio_file
LOCAL_STT_MODEL=

# Post-processing provider: auto | openai | ollama | rules | none
POSTPROCESS_PROVIDER=auto
LLM_BASE_URL=https://api.openai.com/v1
LLM_API_KEY=
LLM_MODEL=gpt-4.1-mini
OLLAMA_BASE_URL=http://127.0.0.1:11434
OLLAMA_MODEL=qwen3:4b

# Desktop insertion: paste | copy
INSERT_MODE=paste

# Mobile Codex Remote. These settings are used by the desktop agent.
# Comma-separated allowlist. Use `label=/absolute/path` for readable project names.
ECHO_CODEX_ENABLED=true
ECHO_CODEX_WORKSPACES=echo=/Users/john/workspace/projects/echo
ECHO_CODEX_COMMAND=codex
ECHO_CODEX_SANDBOX=workspace-write
# ECHO_CODEX_MODEL=
# ECHO_CODEX_PROFILE=
ECHO_CODEX_TIMEOUT_MS=1800000
